[package]
name = "xtls"
version = "0.1.0"
edition = "2021"

[dependencies]
rustls = "0.23.5"
webpki-roots = "0.26.1"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[profile.release]
panic = "abort"
codegen-units = 1
lto = true
opt-level = "s"
strip = true